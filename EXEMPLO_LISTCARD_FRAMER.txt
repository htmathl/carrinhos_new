// Exemplo de como modificar o ListCard.tsx para usar Framer Motion

"use client"

import { useState } from "react"
import { MoreVertical, ShoppingBag, Trash2, Edit, ArrowRight } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import AnimatedDropdown from "./AnimatedDropdown"
import AnimatedCard from "./AnimatedCard" // ← Adicionar esta import
import { useAppStore } from "../store/useAppStore"
import type { ShoppingList } from "../types"
// ... outras imports

export default function ListCard({ list }: ListCardProps) {
  const { deleteList, getListItems, updateList, items } = useAppStore()
  const [isDeleting, setIsDeleting] = useState(false)
  
  // ... resto do código existente...

  const handleConfirmDelete = () => {
    setIsDeleting(true)
    // Aguarda a animação antes de deletar
    setTimeout(() => {
      deleteList(list.id)
      setShowDeleteConfirm(false)
    }, 200)
  }

  return (
    // Substituir o div com animação CSS por AnimatedCard
    <AnimatedCard 
      isVisible={!isDeleting}
      className="w-full"
    >
      <div className="bg-gray-950 border border-gray-800 rounded-xl p-4">
        {/* Todo o conteúdo do card aqui - sem mudanças */}
        <div className="flex items-start justify-between mb-3">
          {/* ... conteúdo existente ... */}
        </div>
        {/* ... resto do conteúdo ... */}
      </div>
    </AnimatedCard>
  )

  // Os dialogs ficam fora do AnimatedCard
  return (
    <>
      <AnimatedCard isVisible={!isDeleting} className="w-full">
        {/* Conteúdo do card */}
      </AnimatedCard>
      
      {/* Dialogs - fora da animação */}
      <Dialog open={showDeleteConfirm} onOpenChange={setShowDeleteConfirm}>
        {/* ... dialogs existentes ... */}
      </Dialog>
    </>
  )
}
